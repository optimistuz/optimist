/**
 * ТЕКСТЫ ПАСПОРТА ПОСАДКИ — отдельным модулем, и это НЕ вкусовщина.
 *
 * ⚠️ Они жили в `home.ts`, и это стоило критическому пути «/» 215 байт
 * (нашёл `hronometrist`): сам блок паспорта грузится лениво, а строки
 * ехали ВСЕМ — `home.ts` целиком лежит в критическом чанке главной.
 * Бюджет «/» израсходован до последнего байта (docs/perf-budgets.md),
 * поэтому текст, который видит только дошедший до камеры, обязан ехать
 * только к нему.
 *
 * Импортировать ТОЛЬКО из ленивых чанков (`fit-passport-block.tsx`).
 * Импорт отсюда в любой модуль критического пути возвращает те же байты.
 *
 * Содержательно: здесь только констатация размера. Обещаний посадки
 * («вам подойдёт…») нет и быть не может — они ждут подписи оптометриста
 * (CLAUDE.md, «Витрина» п. 6; §10 Q9). Формулировки намеренно скучные:
 * замер — это документ, а не комплимент.
 */
export const fitPassportCopy = {
  title: "Ваши миллиметры",
  pd: "Межзрачковое расстояние",
  faceWidth: "Ширина лица",
  widthStep: "Ваша ширина оправы",
  // Допуск показывается ВСЕГДА и рядом с числами, а не мелким шрифтом внизу.
  // Отдельная строка ПОД ступенью: два числа выше — замер, ступень —
  // вывод из допущения. Без этой оговорки допущение выдаётся за геометрию.
  stepNote:
    "Ширина оправы посчитана по ширине лица. Какая сядет именно на вас — проверяют в салоне: нос и уши у всех разные.",
  tolerance: "Замер по камере — ориентир, не рецепт: погрешность ±2 мм.",
  // Ссылка рядом с числами (не кнопка внизу экрана): оговорка без
  // действия оговоркой не работает.
  doctorCta: "Записаться на замер",
  doctorNote:
    "Точное межзрачковое расстояние снимает оптометрист прибором — на приёме это пара минут.",
  // Кадров для замера не хватило: честное отсутствие вместо выдуманных мм.
  absent:
    "Миллиметры снять не удалось — для замера нужно смотреть прямо в камеру. Форму лица это не отменяет.",
} as const;
