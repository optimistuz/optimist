/** Опубликованный артикул, кадры лежат на диске в ОБЕИХ ширинах — ГОДЕН.
 *
 *  Эта ловушка доказывает ПОЛОЖИТЕЛЬНЫЙ путь: гейт, который только падает,
 *  не доказывает ничего — он мог бы браковать вообще всё. Кадры синтетические
 *  (белый прямоугольник, сгенерирован sharp), ничьей съёмкой не являются
 *  и на сайт не идут: фикстуры лежат вне `src/` и вне `public/`. */
import { validFrame } from "./base.mjs";

/** Свой корень статики: боевой public/ трогать нельзя. */
export const publicDir = "_public";

export const frames = [validFrame({ status: "published", photoSet: ["front"] })];
