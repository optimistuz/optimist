# docs/perf-budgets.md — пер-маршрутные бюджеты производительности

> **Статус:** реестр-закон (§3-10 plan.md, «Стандарт качества» CLAUDE.md).
> Заведён этапом 0. **Пополняется КАЖДЫМ новым маршрутом** — витрина
> (`/collections/*`, `/frames/*`), примерка. Каждый каталожный/точностный
> этап сверяет свой маршрут с этой таблицей ПЕРВЫМ пунктом приёмки (§0-12).
> Эталонные устройства аудитории — **Galaxy A15 / A54** (средний Android
> Ташкента, LTE). Числа — это потолки, а не цели «в среднем».

---

## 1. Глобальные бюджеты (действуют на всех маршрутах)

| Метрика | Потолок | Как мерить |
|---|---|---|
| Lighthouse Performance (mobile) | **≥ 90** | Lighthouse, mobile preset, CPU 4× |
| LCP (CPU 4× + LTE) | **≤ 2,5 с** | Lighthouse / trace |
| LCP-элемент | **всегда `img`** (никогда canvas/текст) | Lighthouse «Largest Contentful Paint element» |
| CLS | **≤ 0,05** | Lighthouse |
| Вечные анимации на странице | **ровно 2** (hero-подсказка, лента брендов) | ревью кода |
| Контентные стеклянные поверхности в кадре | **≤ 2** (хром навигации — вне бюджета) | ревью |
| Пик декодированной памяти изображений | **≤ 300 МБ** | DevTools Memory / LRU-гейт кода |

**Заимствованные зависимости (потолки веса, gz):** lazy-бандл оптики
(ogl + шейдеры) **+≤60 КБ суммарно**; критический путь первой отрисовки
**+≤5 КБ gz** к отметке этапа 0 (`next-view-transitions` в layout +
инфраструктура тикера/useGaze); ogl и Kawase — только lazy-чанки.

Факт на этапе 5 (`66d3192`, замер по СОБРАННЫМ чанкам, не по коду):
`ogl` — **14 190 Б gz**, чанк линзы (шейдер + диспетчер) — **3 570 Б gz**,
итого **17 760 Б** из 60 КБ. В критическом пути «/» ни того, ни другого нет.
Запас на Kawase-шторку этапа 6 — ~43 КБ.

---

## 2. Пер-маршрутная таблица

Колонки: **first-load JS** (gz, из вывода `next build`) · **вес до
взаимодействия** · **особые гейты маршрута**. Ячейка `TBD` — заполняется
этапом, вводящим маршрут.

| Маршрут | Вводит этап | First-load JS | Вес до взаимодействия | Особые гейты |
|---|---|---|---|---|
| `/` (главная) | база / этап 0 | см. §3 «база» | 1,04 МБ (факт этапа 11) | критический путь +≤5 КБ gz к базе; LCP-img — hero |
| `/privacy` | 11 | **168 КБ JS по сети** (не 87,6 — см. ⚠️ ниже) | **266 КБ** | статический текст, ноль эффектов; LCP-элемент — логотип шапки (`img`) |
| `/collections/[slug]` | 12 | TBD | ≤ 1,5 МБ | SSG, Product/Collection JSON-LD, OG |
| `/frames/[slug]` (PDP) | 12 | TBD | **≤ 1,5 МБ до взаимодействия** | rack-focus секвенции (см. §3), spin по pointerdown |
| Примерка (камера) | 7 / 14 / 15 | TBD (lazy) | — | пакет+модель только по клику; 3D-чанк ≤700 КБ; GLB ≤1,2 МБ/SKU |

⚠️ **Таблица `next build` ЗАНИЖАЕТ first-load для всех маршрутов, кроме «/».**
Для `/privacy` она печатает **87,6 КБ** (shared + 142 Б сегмента) — и это не
то, что качает телефон. Клиентский хром лежит в корневом `layout`, и его чанки
в колонку сегмента не попадают, хотя в HTML маршрута стоят их `<script>`-теги:
Motion 43,6 + контент `home.ts` 11,9 + Lenis 5,1 + шапка/подвал/рельс 2,9 + …
**Замер сетью (чистый кэш, 390×844, CPU 4×, LTE): 168,2 КБ скриптов.**
Витрина этапа 12 платит этот налог ДО первого килобайта товара — считать вес
маршрута ТОЛЬКО сетевым трейсом или по `<script>`-тегам собранного HTML,
цифре из таблицы не верить.

### Секвенции rack-focus (гейт `build-seq.mjs` — ПАДАЕТ при превышении)

⚠️ **Этот раздел читает машина.** `scripts/build-seq.mjs` парсит ЕГО (от этого
заголовка до следующего) и берёт числа отсюда — в коде их нет. Меняете потолок
здесь — меняется поведение гейта. Ломаете формат — скрипт **останавливается**
и не собирает секвенцию «на глаз». Строки таблицы: `| mobile | 24 | ≤ 45 КБ |`.
Числа проверяются на правдоподобие (кадров 12–60, потолок 10–150 КБ) — опечатка
вроде «450 КБ» гейт не поднимет, а уронит.

| Платформа | Кадров | Потолок на кадр |
|---|---|---|
| mobile | 24 | **≤ 45 КБ** |
| desktop | 30 | **≤ 80 КБ** |

Ширина кадра: mobile **1080 px**, desktop **1600 px**. Исходная серия обязана
дотягивать до бо́льшей: серия в 1200 px не даст «десктопного» набора — она даст
набор шириной 1200 px с именем desktop. Скрипт это проверяет и падает.

Набор целиком: mobile **≤ 1,05 МБ**, desktop **≤ 2,34 МБ**. ⚠️ Рабочий потолок —
**произведение столбцов** (24 × 45 = 1080 КБ), а эти округлённые числа парсятся
независимо и служат ПЕРЕКРЁСТНОЙ ПРОВЕРКОЙ на опечатку: разойдутся с
произведением больше чем на 2 % — сборка встанет. Брать округление за рабочий
потолок нельзя: набор из 24 легальных кадров по 45 КБ весит 1080 КБ и получил бы
ложный СТОП от «1,05 МБ» = 1075 КБ.

Постер секвенции: **1080 px**, **≤ 90 КБ**. Постер — единственный кадр
секвенции, попадающий в «вес до взаимодействия» (страница показывает его
в покое), поэтому у него собственный потолок, и он тоже гейт.

Оба набора **выбираются** из ОДНОЙ снятой серии — промежуточная плоскость
фокуса не интерполируется, поэтому исходников нужно **≥ 30**
(`docs/photo-brief.md` §5).

**Политика загрузки (гейт этапа 12, зафиксирован до написания PDP):**
mobile-набор грузится **по жесту/скроллу, не вперёд**. Замер хронометриста:
front + постер + JS/CSS ≈ **0,58 МБ** (влезаем); если тянуть все 24 кадра
вперёд — **1,63 МБ** по потолкам этой же таблицы (24 × 45 КБ), мимо потолка
1,5 МБ. Спин — только по `pointerdown`, ступенчато (сперва каждый 6-й кадр,
остальные доливаются).

### Packshot и спин (гейт `ingest-frames.mjs` — ПАДАЕТ при превышении)

⚠️ Тот же договор: раздел читает машина, числа берутся отсюда.

| Ассет | Ширина | Потолок |
|---|---|---|
| packshot | 1200 | **≤ 140 КБ** |
| packshot2x | 2400 | **≤ 320 КБ** |
| macro | 1200 | **≤ 200 КБ** |
| macro2x | 2400 | **≤ 600 КБ** |
| spin | 1200 | **≤ 70 КБ** |

Спин целиком: **≤ 3,3 МБ** (48 × 70 КБ). Как и у секвенций — независимая
перекрёстная проверка на опечатку в потолке кадра.

⚠️ **У макро — свой бюджет, и это не поблажка.** В обычном пакшоте оправа
занимает 13–25 % кадра, остальное — чистый белый свип, который почти ничего
не весит. На макро шарнира объект занимает половину кадра и больше, а havana-
ацетат — высокоэнтропийная текстура, ровно то, чего JPEG не жмёт. Замер:
`front` @2400 — 118 КБ, `quarter` — 215 КБ, `macro` — **472 КБ**. Один потолок
на все семь кадров означал бы, что безупречное макро валит артикул целиком.
`macro-1` и `macro-2` берут строки `macro`/`macro2x`, остальные пять —
`packshot`/`packshot2x`.

**Ширина — контракт, а не потолок.** Исходник обязан дотягивать до объявленной
ширины: сдача в 900 px даёт `front.jpg` и `front@2x.jpg` байт-в-байт
одинаковыми, каталог считает, что лесенка есть, телефон качает одно и то же
дважды, а лупа ×1,85 растягивает 900 px в кашу. Приёмка это ловит и бракует
артикул с указанием реальной ширины.

Packshot публикуется **лесенкой**: `<кадр>.jpg` (1200 px) — карточка и LCP;
`<кадр>@2x.jpg` (2400 px) — лупа (`ZOOM = 1.85`) и десктопный зум. Причина:
кадры нормализованы под multiply, а значит идут с `unoptimized` — next/image
их НЕ ужмёт и не отресайзит, телефон скачает ровно то, что положил конвейер.
Один файл на 2400 px в LCP-слоте — это ~230 КБ сети и 16,5 МБ декода на A15.

Качество: q92 + **4:4:4** (без цветового сабсэмплинга — иначе под multiply
на кромке оправы цветная бахрома). Пол качества при подгонке под потолок —
q84: ниже конвейер не жмёт, а честно падает. **Превышение веса — это НЕ
пересъёмка:** кадр не жмётся → уменьшайте ширину/качество или поднимайте
потолок здесь. Фотографа за вес не гоняют.

**Политика загрузки галереи (гейт этапа 12):** из семи пакшотов вперёд грузится
**только `front`**; остальные шесть — по жесту (свайп/клик по превью). Жадная
загрузка всей галереи — **1,52 МБ по потолкам этой же таблицы** (5 × 140 КБ +
2 × 200 КБ макро + база), то есть **уже ЗА потолком** 1,5 МБ, а не «впритык»:
запаса на предзагрузку `quarter` НЕТ. Файлы `@2x`
не грузятся НИКОГДА до жеста зума: один такой в первом экране (+320 КБ) выносит
PDP за бюджет.

> Потолки packshot/spin сняты на апскейле, потолки macro — модель настоящего
> кадра. И те и другие **перемерить на первых реальных кадрах** и, если надо,
> поднять здесь: это ожидаемо, а не ЧП. Занижать нельзя — ложное падение
> в день съёмки дороже лишних килобайт.

### Память декодированных изображений

Пик декодированной памяти PDP **≤ 300 МБ**. LRU ImageBitmap **≤ 12 кадров
НА ПОВЕРХНОСТЬ** (спин, секвенция, галерея — каждая со своим LRU). Это не
придирка: один общий LRU на 12 смешанных элементов даёт трэшинг — скраб спина
вытеснит галерею, галерея вытеснит секвенцию, и всё поедет декодироваться
заново на A54. Настоящие едоки — спин (48 кадров ≈ 176 МБ, если декодировать
целиком), галерея из 7 packshot@2x (≈ 103 МБ) и секвенция (≈ 71 МБ). Без LRU
худший честный сценарий даёт **≈ 350 МБ** — мимо. С LRU 12 на поверхность —
**≈ 182 МБ**, запас есть.

### 3D-примерка (этап 15, за гейтом §10)

- 3D-чанк (three + GLTFLoader + Draco + KTX2) **≤ 700 КБ gz**.
- GLB **≤ 1,2 МБ / SKU** (гейт в `validate-catalog.mjs`); ≤ 20k треугольников; KTX2 + Draco.

---

## 3. База этапа 0 (отметка отсчёта — войдёт в приёмку этапа 9)

Снято на этапе 0 до любых визуальных правок. Все дальнейшие «+КБ» бюджеты
считаются ОТ этих чисел. Валидирует и подтверждает — агент `hronometrist`.

### ⚠️ МЕТОД ЗАМЕРА — ЕДИНСТВЕННЫЙ ДОПУСТИМЫЙ (закрыт этапом 5)

Округление «192 КБ» / «197 КБ» дважды приводило к спору, есть ли у главной
запас. Спор закрыт: числа ниже — **точные байты**, метод воспроизводит
`next build` бит-в-бит.

- **First Load JS «/» = сумма gzip-размеров УНИКАЛЬНЫХ `.js` из
  `.next/app-build-manifest.json` для ключа `/page`.**
- **gzip уровня 9, а не 6.** Это и была причина расхождения двух методов
  (~500 Б на 197 КБ): уровень 6 даёт 197 701 Б там, где Next печатает
  «197 kB», а 197 701 Б округлился бы в «198». Проверка метода: при уровне 9
  shared-чанки дают 87 484 Б — ровно «87,5 kB» из вывода Next.
- **«КБ» в этом реестре = 1000 байт**, потому что так печатает Next (`kB`).
  Читать «197 КБ» как 197 × 1024 = 201 728 Б **запрещено**: база этапа 0
  никогда не была 192 × 1024, она была 191 843 Б. Прочтение в КиБ
  подарило бы главной 4,7 КБ бюджета, которого не существует.
- Мерить **прод-сборкой в ИЗОЛИРОВАННОМ дереве** (`git worktree` + junction
  на `node_modules`), а не в рабочей папке: `next build` и живой `npm run dev`
  делят `.next`. Дерево этапа 0 (`9072cad`) в такой сборке воспроизводится
  один-в-один — «34 kB / 192 kB», что и подтверждает пригодность стенда.

| Метрика (маршрут `/`) | База этапа 0 | Инструмент |
|---|---|---|
| First-load JS | **191 843 Б gz** (Next печатает «192 kB»; 34 КБ маршрут + 87,4 КБ shared) | `next build` (Next 14.2.35), дерево `9072cad` |
| LCP (mobile, CPU 4×) | **≈ 4,7 с** (Lighthouse simulate LTE; CDP реальн. троттлинг — 2,4 с; LCP-элемент = hero `img`) | Lighthouse |
| Lighthouse Performance (mobile) | **66–74** (2 прогона; варьирует по TBT) | Lighthouse |
| Консоль первого захода на `/` | **чисто — 0 ошибок, гидрация в порядке** | `diag-firstload.mjs` |

### Факт по этапам (маршрут `/`, first-load JS)

База §3 не пересматривается — здесь фиксируется фактический замер на закрытии
этапа. Бюджет критического пути: **+≤5 КБ gz к базе этапа 0**. Замеряет
`hronometrist` прод-сборкой (`next build`), НЕ при живом `npm run dev`.

**ПОТОЛОК В БАЙТАХ (единственная рабочая формулировка):**
191 843 + 5 000 = **196 843 Б gz**. Всё, что выше, — пробой, даже если
`next build` печатает «197 kB»: печать округляет, бюджет — нет.

| Этап | First-load JS | Δ к базе (192 КБ) | Вердикт |
|---|---|---|---|
| 0 (база) | 192 КБ (34 маршрут + 87,4 shared) | — | отметка отсчёта |
| 4 «Зона ясного зрения» | **195 КБ** (36,4 маршрут + 87,4 shared) | **+3 КБ** (унаследовано этапом 3; сам этап 4 = ±0) | в бюджете |
| Хотфикс «Ф» «Событие подбора» | **196 КБ** (36,9 маршрут + 87,4 shared) | **+4 КБ** | в бюджете — **остался 1 КБ** |
| 10 шаг 7–8 «Конвейер кадров» | **196 КБ** — без изменений | **+4 КБ** | конвейер живёт вне бандла (`node scripts/…`), в `src/` не входит ✓ |
| 11 «Паспорт оправы» | **196 КБ** (37,1 маршрут + 87,4 shared) | **+4 КБ** | схема, гейт и политика в «/» не входят ✓ (см. ловушку ниже) |
| 12а с1 «Витрина: каталог+хром» | **197 КБ** (маршрут «/» ~37,1; +1 КБ — клиентский манифест маршрутов Next) | **+5 КБ — на потолке (запаса 0)** | новые `/collections` растят клиентский манифест маршрутов, он шлётся на ВСЕ страницы вкл. «/»; убрать нельзя без удаления маршрутов; формально ≤, но запаса больше нет (913e7c1). ⚠️ Число ОКРУГЛЁННОЕ и оно врало — точный замер ниже |
| 7 + 12а (дерево `b1ba8bd`, точный замер) | **197 081 Б** | **+5 238 Б** | 🔴 **ПРОБОЙ +238 Б.** Вскрылся только байт-точным методом: «197 kB» в печати Next скрывало превышение. Долг НЕ этапа 5 — он пришёл с 7 и 12а |
| 5 «Честная линза» (`123039f`) | **197 144 Б** | **+5 301 Б** | 🔴 **ПРОБОЙ +301 Б.** Собственная цена этапа 5 — **+63 Б** (расщепление триггер/логика/ogl сработало) |
| 5 + починка физика (`66d3192`) | **197 156 Б** | **+5 313 Б** | 🔴 **ПРОБОЙ +313 Б.** Починка стоила ещё +12 Б; ogl и шейдер в ленивых чанках: 14 190 + 3 570 = **17 760 Б gz** при потолке 60 КБ ✓ |
| 5 шаг 7 «Линза по чертежу» (`3f81a6d`) | **196 881 Б** | **+5 038 Б** | 🟠 **ПРОБОЙ +38 Б** — сокращён с +353 Б. Шаг ВЕРНУЛ вес: **−315 Б** (удаление proximity-подсветки окупило линзу). ⚠️ Число перенесено из сообщения коммита `3f81a6d` постфактум (дирижёр, 19 июля): сам коммит реестр НЕ правил, хотя канон требует «числа веса живут ТОЛЬКО здесь». **Подлежит переизмерению базовым прогоном этапа 6** — до переизмерения считать ориентиром, а не фактом |
| 5 «Дисперсия по источнику» (`cf8842a`) | не измерялся | — | правка двух uniform-констант в ленивом чанке линзы; критического пути «/» не касается. Реестр закрывается замером этапа 6 |
| **6 шаг 0 — отметка базы** (`cf8842a`) | **196 883 Б** | **+5 040 Б** | 🟠 **ПРОБОЙ +40 Б.** Переизмерение подтвердило ориентир строки выше (196 881 → 196 883, разница 2 Б — правка дисперсии). Долг унаследован с этапов 7 и 12а, этап 6 его не создавал |
| 6 шаг 1 «Непрерывный расфокус» | **196 874 Б** | +5 031 Б | 🟠 пробой +31 Б. Снятие квантования вернуло **−9 Б**: машинерия ступеней оказалась дешевле, чем ожидалось |
| 6 шаг 2 «Kawase» (до чистки) | 197 200 Б | +5 357 Б | 🔴 пробой +357 Б. Цена: glue `next/dynamic`, разветвление GL/CSS **и дублирующая проба WebGL** |
| 6 шаг 2 (после возврата дубля) | **197 086 Б** | +5 243 Б | 🔴 пробой +243 Б. **−114 Б**: проба WebGL удалена из критического пути — такая же живёт в ленивом чанке линзы, а слой Kawase и так сообщает о неудаче через `onFail` |
| **6 «Разрез vision-sim» — ПОДТВЕРЖДЁН ХРОНОМЕТРИСТОМ** | **196 028 Б** (мой независимый прогон `80718d1`) | **+4 185 Б** | 🟢 **В ПОТОЛКЕ, ЗАПАС 815 Б.** Перемер тем же методом (gzip-9, уникальные `.js` из `app-build-manifest` для `/page`, dev погашен, изолированное дерево). Расхождение с числом исполнителя — **37 Б** (196 065 против 196 028); контрольный замер дерева этапа 5 `cf8842a` тем же прибором дал **196 840 Б** против объявленных 196 883 Б, то есть смещение **систематическое (~40 Б), а не случайное** — разные сборочные прогоны одного дерева. **ВЕРДИКТ НЕ МЕНЯЕТСЯ ни при каком из двух отсчётов, и Δ разреза воспроизвелась: −812 Б у меня против −1021 Б заявленных** (моя дельта считается от `cf8842a` = 196 840, заявленная — от 196 883). ⚠️ По МОЕМУ отсчёту унаследованный долг этапов 7 и 12а был не +40 Б, а **−3 Б (ровно на потолке)**; считать запас консервативно — **778 Б**, а не 815 |
| **6 второй круг починок (нормировка на плотность, расписание цепочки, ресайз, ARIA)** | **196 050 Б** (замер исполнителя, dev погашен, gzip-9, уникальные `.js` из `app-build-manifest` для `/page`) | **+4 207 Б** | 🟢 **В ПОТОЛКЕ, ЗАПАС 793 Б.** Критический путь НЕ вырос: 196 065 → 196 050 (−15 Б). Из оболочки `vision-sim.tsx` ушло ветвление по знаку для ARIA, новый модуль `lib/kawase-schedule.ts` уехал в ЛЕНИВЫЙ чанк вместе с `kawase`. Число в пределах известного систематического смещения прогонов (~40 Б) — подлежит перепроверке хронометристом |
| **6 шаг 6 — колесо мыши** | **196 049 Б** (замер исполнителя, dev погашен, gzip-9) | **+4 206 Б** | 🟢 **В ПОТОЛКЕ, ЗАПАС 794 Б.** Дельта к предыдущему замеру **−1 Б**: обработчик колеса, троттлинг и атрибут `data-lenis-prevent-wheel` целиком уехали в ЛЕНИВЫЙ чанк `vision-sim-live`, а `useOpticalCapability` уже был в критическом пути (его тянет hero). Лимит «ноль байт» выдержан |
| 6 «Разрез vision-sim» (замер исполнителя) | 196 065 Б | +4 222 Б | 🟢 в потолке, запас 778 Б. Разрез вернул **−1021 Б** и закрыл не только долг этапа 6, но и унаследованные +40 Б. Граница разреза — вердикт дирижёра: в HTML остаются кадр настоящим `<img>` (LCP-элемент), зарезервированная пропорция (CLS), подписи и ARIA-каркас ползунка в ОДНОМ экземпляре; лениво уезжает машинерия (перетаскивание, клавиатура, motion-значения, Kawase). ⚠️ Вариант «весь файл в `ssr:false`» ОТКЛОНЁН дирижёром: он убрал бы фото из HTML — пустая коробка на LTE и прыжок вёрстки при гидрации, то есть размен байтов на регрессию метрик |

### Рантайм-замер этапа 6 «Субъективная камера» (Kawase-шторка) — hronometrist, 20 июля 2026

Стенд: Chrome **головной** (настоящий GPU; swiftshader соврал бы про цену
шейдера), 390×844, `deviceScaleFactor` 3 → DPR-кап 2, CPU 4×, прод-сборка
дерева `80718d1` в изолированном worktree на порту 3100. Контроль — дерево
этапа 5 `cf8842a` на 3101. **Дев на :3000 в замерах не участвовал:** каждый
прогон сверяет ХЭШ маршрутного чанка (`page-3e632dc2a7d2fdb1.js` против
`page-23d50377fef6dbea.js` у контроля), а dev отдаёт неверсионированный
`chunks/app/page.js` — подмена стенда невозможна по построению.

⚠️ **ДВА ДЕФЕКТА ПРИБОРА, НАЙДЕННЫЕ ПО ХОДУ (иначе замер соврал бы).**
(1) Поток из ~20 000 CDP-событий за 5 минут сперва ПОДВЕШИВАЛ прибор, а
затем гасил кадр страницы на 96-й секунде из 331 — «деградации после
прогрева нет» было бы выводом об ОСТАНОВЛЕННОЙ странице. Свип переехал
ВНУТРЬ страницы (настоящий `touchStart` через CDP, дальше `pointermove`
из её же rAF), и покрытие стало полным. **Прибор обязан печатать
фактическое покрытие кадрами — иначе оборванный прогон неотличим от
чистого.** (2) Счётчик отрисовок патчил только `WebGLRenderingContext`,
а ogl поднимает **WebGL2** — при работающем канвасе он честно печатал НОЛЬ.

#### Таблица времени кадра при перетаскивании шторки

Жест: непрерывный свип 0 ↔ максимум, полупериод 2 с (радиус меняется
каждый кадр — худший честный случай).

| Состояние | покрытие | медиана | средн | p90 | p99 | >33 мс | fps(средн) |
|---|---|---|---|---|---|---|---|
| верх страницы, шторки нет в кадре | 20 с | 20,0 мс | — | 20,1 | — | 0,1 % | ~50 |
| шторка В КАДРЕ, ни одного касания | 60 с | 20,0 мс | — | 20,1 | — | 0,1 % | ~50 |
| **01 «Даль» (street, −6 дптр, depthMask)** | 96 с | **33,2 мс** | 28,3 | 49,9 | 66,6 | 17,0 % | **35,3** |
| **02 «Близь» (book, +3 дптр)** — оба канваса живы | **201,7 с** | **44,9 мс** | 39,3 | 57,2 | 80,1 | 53,9 % | **25,4** |
| 01 «Даль», полные 5,7 мин | **342,9 с** | **40,0 мс** | 50,8 | 60,1 | 420,1 | 65,8 % | **19,7** |

Поминутно в 5,7-минутном прогоне (мин. 1→6, fps по среднему):
**24,3 · 23,8 · 29,6 · 22,8 · 5,3 · 7,8**; longtask **1 115 шт / 143,1 с**.

🔴 **60 fps НЕ ДОСТИГНУТЫ НИ В ОДНОМ ПРОГОНЕ.** Шторка под жестом идёт
**25–35 fps**, а когда в кадре живы ОБА симулятора — **25 fps**. Это тот же
порядок, что у линзы этапа 5 (33,2 мс), и получен на стенде с ДЕСКТОПНЫМ
GPU: на Adreno/Mali A15–A54 запас может быть только меньше.

🟠 **«Деградации после прогрева нет» ПОДТВЕРДИТЬ НЕЛЬЗЯ.** Минуты 1–4
ровные (24,3 / 23,8 / 29,6 / 22,8 fps), минуты 5–6 обвалились до 5,3 и
7,8 fps при p99 420–880 мс. В прогоне «Близь» (201,7 с) обвала не было
(39,1 / 39,8 / 39,4 / 38,4 мс — ровно). Обвал НЕ ВОСПРОИЗВЕДЁН дважды и
на этом стенде параллельно жили другие Chrome и два сервера — **это
кандидат в находку, а не находка**: нужен чистый повтор, лучше сразу
на живом A54. Записано именно так, чтобы следующая сессия не приняла
ни «всё ровно», ни «продукт деградирует» без доказательства.

#### Живой шаг клапана (§8: «клапан доказывается ПАДЕНИЕМ, а не проводкой»)

Клапан **ЖИВОЙ и шагает под нагрузкой** — в отличие от этапа 5, где он был
слеп. Первый шаг на **t = 3,5 с**, ход `ratio` 2,0 → 1,699 → 1,398 → 1,099
→ **1,0**, то есть scale 1,0 → **0,5 = АБСОЛЮТНЫЙ ПОЛ**.

🔴 **У КЛАПАНА КОНЧИЛСЯ ХОД, И ХОД НИЧЕГО НЕ КУПИЛ.** Медиана ДО первого
шага — 40,0 мс, ПОСЛЕ — 40,0 мс; на установившемся полу (`ratio` 1,0) —
**79,9 мс**, то есть ХУЖЕ. Разрешение упало вчетверо по площади, время
кадра не улучшилось. Это ровно случай §8-4: «упёршийся в пол клапан НЕ
является доказательством, что виноват именно этот канвас». **Вторая мера
(радиус / упрощение шейдера) заблокирована §8 до прогона на живом A54** —
здесь эмулируется придушенный CPU, а не слабый GPU. Device-долг,
открытый этапом 5, теперь держит ДВА этапа.

🔴 **ПОЛ «ДИНАМИЧЕСКИЙ» — И ОТ ЭТОГО РАЗРЕШЕНИЕ ДРЕБЕЗЖИТ.** Распределение
`ratio` за 5,7 мин: `{1,398: 66, 1,099: 66, 1,0: 4, 1,699: 1, 2,0: 1}` —
**138 смен разрешения, 0,4 раза в секунду, бесконечно**. Механика:
`vision-blur.tsx:113` держит пол 0,5 при s ≥ 0,25 и 0,7 иначе, а
`render-scale.ts:214-218` (`setMin`) поднимает scale обратно МГНОВЕННО,
тогда как вниз он идёт шагами по 3 с. Палец пересекает 1,5 дптр каждые
~1,5 с — и scale пилит 0,55 ↔ 0,70. **Каждая смена — ПОЛНАЯ пересборка
цепочки FBO** (`gl-canvas.tsx:237` → `vision-blur.tsx:90` →
`kawase.ts:163`: `dropTargets()` + три новых `RenderTarget`, 115 756 Б
GPU-текстур). 138 циклов «удалить/выделить» за прогон, в самый нагруженный
момент взаимодействия.

#### Вечный цикл: шторка рисует, когда её никто не трогает

| Состояние | отрисовок GL | канвасы |
|---|---|---|
| секция ВНЕ экрана | **0** (0/с) | нет; WebGL-контекстов запрошено **0** ✓ |
| секция В КАДРЕ, ноль касаний, 60 с | **2 392** | **39,9 отрисовки в секунду** |

🔴 `vision-blur.tsx:146` **НЕ передаёт `shouldRender` в `GLCanvas`** — в
отличие от линзы (`lens-gl.tsx:298`: `() => stateRef.current.opacity > 0.001`).
Гейт в `gl-canvas.tsx:267` существует, но у шторки пуст, поэтому петля
каждый кадр трогает layout (`host.clientWidth`), кормит клапан и гонит
Kawase по НЕИЗМЕНИВШЕЙСЯ картинке. Косвенное подтверждение из первого
прогона: клапан шагнул вниз **в чистом покое**, без единого касания.
Ленивый подъём и паузы работают (вне экрана — ноль, `document.hidden`
проверяется), но «в кадре и не трогают» — это не жест, не появление и не
скролл, то есть третья вечная анимация сверх бюджета «ровно ДВЕ».
Чинится одной строкой: гейт по изменению severity/clip + один
финальный кадр.

#### Память (потолок 300 МБ)

Реальные пиксели декода, а не `naturalWidth`: у `next/image` с
`srcset`+`sizes` он отдаёт плотностно-скорректированный размер и занижал
итог в ~9 раз (390 px вместо 1200 px).

| Статья | Объём |
|---|---|
| 15 изображений «/» (deco-2/deco-3 1600×1067 по 6,51 МБ; hero-портреты 1080×1351 по 5,57 МБ) | **69,8 МБ** |
| буферы двух канвасов шторки (684×514 RGBA) | 2,68 МБ |
| цепочки FBO двух шторок (3 уровня, 0,5×) | **0,22 МБ** |
| GPU-копии текстур двух шторок (street + book @1200) | ≈ 8,3 МБ |
| JS heap | 10 МБ |
| **ИТОГО** | **≈ 91 МБ из 300 МБ — 30 %** ✓ |

**Собственная цена этапа 6 по памяти — ≈ 11,2 МБ.** Утечки нет: heap
9→10 МБ за 5,7 мин, 10→10 МБ в контрольном прогоне. Текстура берётся из
уже декодированного `<img>` секции (`vision-blur.tsx:78`) — второй
загрузки нет, в отличие от линзы этапа 5.

#### Загрузка «/» против дерева этапа 5 (один стенд, кэш выкл, LTE 1,6 Мбит/с + RTT 150 мс, CPU 4×)

| Дерево | FCP | LCP | LCP-элемент | TBT | CLS | Сеть |
|---|---|---|---|---|---|---|
| `cf8842a` (этап 5) | 2 996 мс | 8 476 мс | `IMG.object-cover` ✓ | 3 181 мс | **0,0000** | 27 запр. / 720 КиБ |
| `80718d1` (этап 6) | **2 816 мс** | **8 576 мс** | `IMG.object-cover` ✓ | **3 288 мс** | **0,0000** | 28 запр. / 917 КиБ |

✅ **CLS РАЗРЕЗА ДОКАЗАН ЗАМЕРОМ, а не только конструкцией: 0,0000 при
НУЛЕ зарегистрированных сдвигов.** Утверждение дирижёра подтверждается:
`vision-sim.tsx:106` держит пропорцию `aspect-[4/3] sm:aspect-[16/9]`,
`:177` — подпись состояния фиксированной `h-6` с абсолютным `<p>`, все
узлы ленивой части `absolute`. Прыгать при гидрации нечему.
✅ LCP-элемент остался `img` — разрез не выбил кадр из HTML.
✅ Шторка ниже первого экрана: `top = 2 608 px` при вьюпорте 844,
**канвасов на первом экране 0**.
🟠 LCP +100 мс и TBT +107 мс — в пределах разброса одиночных прогонов
(FCP при этом на 180 мс ЛУЧШЕ), регрессией не считаю.
⚠️ **+197 КиБ сети НЕ АТРИБУТИРОВАНЫ.** Критический путь по сборке стал
на 812 Б ЛЕГЧЕ, так что это не новый вес бандла; вероятнее — на одно
изображение больше успело догрузиться в 30-секундном окне. Одиночным
прогоном не разделяется; повторить при следующем замере.

#### Ленивый бандл оптики (потолок 60 КБ суммарно)

Сетевой замер по факту пробуждения шторки (как отдаёт сервер):
ogl `444` — 14 633 Б, Kawase + `vision-blur` `50` — 3 202 Б, машинерия
`vision-sim-live` `620` — 2 797 Б, общий `913` — 2 147 Б →
**22 779 Б, 38 % бюджета** ✓. Если проскроллить и Collections (линза
`472` 3 710 Б + `407` 2 061 Б) — **28 550 Б, 48 %** ✓.
По собранным чанкам (gzip-9): ogl 14 190 + Kawase 2 801 + линза 3 314 +
машинерия шторки 2 401 = **22 706 Б**. `ogl` в критическом пути «/»
**отсутствует** (проверено грепом маркеров ogl по чанкам манифеста
`/page`) ✓.

---

### Рантайм-замер этапа 5 «Честная линза» (мобильная эмуляция, 5 минут)

Стенд: Chrome **головной** (не headless — нужен настоящий GPU; swiftshader
соврал бы про цену шейдера), 390×844, `deviceScaleFactor` 3 → DPR-кап 2,
CPU 4×, прод-сборки в изолированных деревьях на портах 3100–3102.
Жест: долгое нажатие 8 с по карточке Collections, цикл 5 минут.
Контроль — то же дерево ДО этапа 5 (`b1ba8bd`), где на таче линзы нет вовсе.

| Состояние | медиана | p90 | p99 | >33 мс | longtask за 300 с |
|---|---|---|---|---|---|
| покой, линза не вызвана (`66d3192`) | 16,7 мс | 16,7 | 33,3 | **1,4 %** | 10 шт |
| контроль `b1ba8bd`, линзы нет | 16,7 мс | 33,4 | 66,6 | 39,6 % | 231 шт / **16,3 с** |
| `123039f`, линза живая | **49,9 мс** | 116,5 | 216,4 | 69,7 % | 2 299 шт / **210 с** |
| `66d3192` (починка), линза живая | **33,2 мс** | 33,4 | 50,1 | 56,0 % | 106 шт / **6,3 с** |

**До жеста линза не стоит ничего** — расщепление триггер/логика/ogl честное:
покой не отличается от страницы без линзы, WebGL-контекстов 0, чанк не качан.

🔴 **С живой линзой кадр встаёт на 33,2 мс — ровно 30 fps, и это стабильная
половина частоты, а не джиттер** (p90 33,4; >50 мс всего 1,2 %). Главный жор
main-thread починка сняла (210 с → 6,3 с longtask), остаток — GPU/композитор:
после исправления viewport заштрихованная площадь выросла ВЧЕТВЕРО
(viewport 342×257 → 684×514), диск линзы — с ~30 тыс. фрагментов до ~121 тыс.
Канон требует 60 fps на A15/A54 технологией, и технология для этого случая
уже стоит в проекте — `renderScale`. Она не сработала (см. ниже).

🔴 **КЛАПАН `renderScale` СЛЕП В ТОЧНОСТИ ТАМ, ГДЕ НУЖЕН.** За все 300 с
активной линзы `canvas.width / host.clientWidth` держался на **2,000**
(scale 1,0) — ни одного шага вниз при медиане кадра 33 мс. Причина в окне
замера (`gl-canvas.tsx`, функция `render`): `t0` берётся ПОСЛЕ `ensureSize()`
и охватывает только `scene.draw()`, а `draw()` — это запись униформ и
`renderer.render()`, то есть выдача GL-команд. Их стоимость — доли
миллисекунды; вся настоящая цена лежит в GPU (асинхронно, за окном) и в
layout до `t0`. EMA видит ~0,2 мс при пороге 8 мс и не шагнёт никогда.
Контракт §8 plan.md («кормить СТОИМОСТЬЮ работы, не интервалом») написан
против ложной деградации на ПРОСТАИВАЮЩЕМ телефоне — и в этой части верен;
но в состоянии «канвас рисует каждый кадр» интервал снова становится честным
датчиком, потому что разрешение на него влияет. Правка контракта — решение
дирижёра, не хронометриста.

### Память и сеть линзы (расчёт + замер)

JS-heap за 5 минут: **8,7–9,9 МБ**, дрейфа нет, утечки нет.
Настоящий едок — не heap:

| На одну «взведённую» карточку | Объём |
|---|---|
| декод исходного JPEG 1448×1086 RGBA | **6,0 МБ** |
| GPU-текстура (мипы выключены) | **6,0 МБ** |
| буфер канваса 684×514 RGBA ×2 | **2,8 МБ** |
| **итого** | **≈ 15 МБ** |

Три карточки Collections — **≈ 45 МБ** при потолке 300 МБ ✓.

⚠️ **Линза качает фото ВТОРОЙ раз.** Карточка отдаётся через `next/image`
(`/_next/image?url=…&w=1080&q=75`, WebP), а текстура берёт сырой
`/photos/<slot>.jpg` — **другой URL, другая загрузка, другой декод**:
+123 КБ сети и +6,0 МБ декода на карточку, по жесту. Лечится текстурой
из уже декодированного `<img>` карточки (лупа ×1,85–2,6 от 1080 px —
проверить резкость) либо явной лесенкой `@2x`, как у packshot.

Загрузка «/» этапом 5 НЕ ухудшена (тот же стенд, кэш выключен,
LTE 1,6 Мбит/с + RTT 150 мс, CPU 4×):

| Дерево | FCP | LCP | LCP-элемент | TBT | CLS | Сеть |
|---|---|---|---|---|---|---|
| `b1ba8bd` | 2,99 с | 10,54 с | `IMG.object-cover` ✓ | 4 028 мс | 0 | 27 запр. / 721 КиБ |
| `66d3192` | 3,22 с | 10,26 с | `IMG.object-cover` ✓ | 4 173 мс | 0 | **27 запр. / 721 КиБ** |

Сеть совпала байт-в-байт: линза не добавляет к загрузке НИЧЕГО, её ассеты
едут только по жесту. ⚠️ LCP 10 с здесь — НЕ то же число, что «4,9 с»
в замере этапа 11 ниже: там Lighthouse-симуляция, здесь реальный троттлинг
CDP с более жёстким каналом. Сравнивать эти строки между собой можно
(один стенд), с Lighthouse-таблицей — нельзя.

Сверка этапа 11 прибором (`next build` на изолированной копии, dev не мешал):
база HEAD `d841a4b` — **36,9** маршрут + 87,4 shared; этап 11 — **37,1** + 87,4.
Рост маршрутного чанка **+0,2 КБ** (строка согласия, `frameLine`, `usePathname`),
First Load держится на 196 КБ. По сети «/» выросла на **+0,8 КБ** (1067,1 →
1067,9 КБ). `usePathname` из `next/navigation` стоит ~0: хуки маршрута уже
лежат в рантайме App Router — в отличие от `next/link` (см. ловушку ниже).

⚠️ **Ловушка этапа 11, стоившая 9 КБ (поймана `next build`, не глазами).**
`next/link` не был импортирован в `src/` НИ РАЗУ: сайт живёт на `<a>` и
`AnimatedLink`. Первый же импорт `next/link` — ссылка на политику в подвале —
затащил в критический путь «/» роутер целиком: **196 → 205 КБ**, мимо потолка
197 КБ. Лечится обычным `<a>`: юридическую страницу открывают раз в жизни,
клиентская навигация ей не нужна. **Пока `next/link` не появился в бандле «/»
осознанно (этап 12, `next-view-transitions`), любой его импорт в компонент
главной = −9 КБ бюджета.**
Вторая мина того же этапа: `booking-form` импортировал строку согласия из
`content/privacy.ts` и утащил бы в «/» ВЕСЬ текст политики. Строка живёт
в `home.ts` (`consentLine`) — рядом с формой, а не рядом с политикой.

⚠️ **Третья мина взведена и не разряжена: zod. Цена утечки — 16,3 КБ gz.**
Замер, а не оценка: серверный роут `api/booking` со сборкой zod весит
**31,0 КБ gz** против **14,7 КБ gz** без него. Проверено по факту чанков
(grep по `.next/static`, не по коду): в клиенте zod **отсутствует целиком** —
он живёт только в `.next/server/app/api/booking/route.js`. Держится это на
двух ниточках: `catalog/schema.ts` (единственный импортёр zod) читают лишь
node-гейт и сервер, а `frames.ts` тянет из него тип **`import type`**, который
стирается сборкой. Один value-импорт схемы в модуль с `"use client"` — и в
бандл витрины падает 16,3 КБ gz: **втрое больше всего бюджета критического
пути (5 КБ)**. Помощники рантайма живут в `catalog/frame.ts` — модуле, который
не знает про zod; импортировать в клиент можно ТОЛЬКО его.

🔴 **КРИТИЧЕСКИЙ ПУТЬ «/» ПРОБИТ: 197 144 Б при потолке 196 843 Б (+301 Б).**
Пробой пришёл НЕ с этапом 5 (его цена — 63 Б): дерево `b1ba8bd` до единой
строки ogl уже стояло на 197 081 Б. Съели бюджет этапы 7 и 12а, а округление
«197 КБ» это прятало — печать Next не различает 196 843 и 197 449.
Витрина дальше входит в «/» уже В МИНУС. Ближайшие источники возврата
(не отключение эффектов, а перенос веса):
- расщепить `home.ts` — навигация и контакты отдельно от текстов квиза и
  `faceFit` (см. ⚠️ про налог хрома выше): в «/» они нужны, в чанк витрины —
  нет, и наоборот;
- проверить, весь ли клиентский манифест маршрутов обязан ехать на «/».
Проверять `next build` байт-точно на КАЖДОМ шаге витрины, а не на закрытии.

### Приборный замер этапа 11 (прод-сборка, Lighthouse mobile, дефолтный пресет)

Меряно на `next start` изолированной копии, чистый кэш, CPU 4× + LTE-simulate.

| Маршрут | Lighthouse | LCP | TBT | CLS | Вес | LCP-элемент |
|---|---|---|---|---|---|---|
| `/` | **69** (база этапа 0: 66–74) | **4,9 с** (база: ≈4,7 с) | 290 мс | 0 | 783 КиБ | hero `img` ✓ |
| `/privacy` | **90** (ровно на полу) | **2,8 с** — мимо потолка 2,5 с | 280 мс | 0 | 261 КиБ | логотип шапки `img` |

Этап 11 «/» не ухудшил: числа совпадают с базой в пределах шума. Пробой LCP
и Lighthouse на «/» — **долг базы, а не этапа 11** (прелоадер + 9 изображений +
207 КБ шрифтов в первом экране); чинится этапом «Скорость», не здесь.

⚠️ **Новорождённый маршрут `/privacy` вышел на нижнюю кромку бюджета: 90 из 90
и LCP уже за потолком.** Виновата не страница (её HTML — 10 КБ текста, ноль
эффектов), а **налог хрома: ~168 КБ JS на КАЖДОМ маршруте** (Motion 43,6 +
`home.ts` 11,9 + Lenis 5,1 + …) и TBT 280 мс на пустом тексте. Витрина этапа 12
входит в свои маршруты уже с этим грузом: у PDP от потолка 1,5 МБ остаётся
~1,23 МБ на товар. Чем лечить (технологией, не отключением): расщепить
`home.ts` — навигация и контакты (нужны хрому всюду) отдельно от текстов квиза
и `faceFit` (нужны только «/»); тексты, которые витрина не рендерит, не должны
ехать в её чанк. Ревизия — до первого маршрута витрины, а не после.

⚠️ **Закон «LCP-элемент — всегда `img`» на текстовом маршруте вырожден.**
На `/privacy` LCP выиграл логотип шапки — формально `img`, фактически случайность:
уменьшите логотип, и LCP станет заголовком-текстом, то есть «нарушением» закона,
написанного для визуальных маршрутов с hero. Правило остаётся в силе для «/»,
`/collections/*`, `/frames/*`; для чисто текстовых юридических страниц оно
не применяется — LCP там честно текст. Формулировку §1 уточнять владельцу
не требуется, но пересматривать её ради «зелёной галочки» на `/privacy` —
запрещено: это подгонка прибора под ответ.

---

## 4. Как мерить (инструменты в репозитории)

Нужен `npm run dev` на `:3000` (для CDP-скриптов) либо `npm run build` (для веса):

- `node scripts/diag-firstload.mjs` — первый заход vs рефреш, консоль/гидрация.
- `node scripts/cdp-audit.mjs` — белая точка FloatFrame (±1).
- `node scripts/ingest-frames.mjs <дерево-сдачи>` — приёмка кадров фотографа
  (белая точка, комплектность, мм). Дев-сервер не нужен.
- `node scripts/build-seq.mjs <rack-dir> --sku <id>` — сборка секвенций.
  **Потолки берёт из таблицы «Секвенции rack-focus» этого файла** (§2), не из кода:
  документ здесь — источник правды, разойтись с ним скрипт не может.
  Падает при превышении бюджета кадра или набора.
- `node scripts/validate-catalog.mjs` — санити каталога и GLB-гейт (появляется этапом 11).
- Lighthouse mobile: DevTools → Lighthouse → Mode «Navigation», Device «Mobile», CPU throttling 4×.
- Мобильная эмуляция: DevTools 390×844 + CPU 4×, прогон тяжёлых эффектов 5+ минут.
