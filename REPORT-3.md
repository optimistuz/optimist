# REPORT-3 — Симулятор близорукости (секция «Зрение»)

Дата: 12.06.2026. Точечный проход: шторка «до/после» заменена на
симулятор близорукости со шкалой 0…−6 дптр с шагом 0,5.

---

## 1. Сводка аудита (блок 3.1)

| Проверка | Результат |
|---|---|
| `npx tsc --noEmit` | ✅ без ошибок (после каждого блока) |
| `npm run lint` | ✅ No ESLint warnings or errors |
| `npm run build` | ✅ зелёный (после каждого блока) |
| First Load JS главной | **164 кБ** против базы 164 кБ (**+0**) |

Размер route-чанка главной вырос с 12,8 до 13,4 кБ (+0,6 кБ — симулятор
чуть больше шторки), итоговый First Load JS не изменился. Ожидание ±2 кБ
выполнено. Новых npm-зависимостей нет.

---

## 2. Сделанное по блокам

### Блок 1 — компонент
`src/components/ui/myopia-sim.tsx` (новый):
- Сцена: два слоя `Photo slot="street"` — резкая база + размываемый
  оверлей (`blur + saturate`, transition 180 мс, `will-change: filter`,
  статическая маска глубины «низ кадра резче»). При D = 0 оверлей
  скрыт (`opacity: 0`) — база пиксельно резкая.
- Калибровка: `B = (|D|/6) × 0.013 × ширина контейнера` (кламп ≤ 18px),
  `S = 1 − |D|/6 × 0.15`. Ширина — из ref + ResizeObserver.
- Значение меняется только ступенями 0,5 (13 позиций) — один пересчёт
  filter на ступень, пофреймового blur при перетаскивании нет.
- Шкала под фото: волосяная линия, деления 0,5/1,0, цифры 0…−6
  (tabular-nums), бегунок 28px `brand` с кольцом `offwhite`, капсула
  «−2,5 дптр» (ru-RU, запятая, один знак) едет с бегунком.
- Управление: drag за бегунок и клик/drag по треку (снап к ступени
  мгновенный, бегунок доезжает spring'ом), `touch-action: none` только
  на треке; клавиатура ←/→ (0,5), Home/End (0/−6), `role="slider"`,
  `aria-valuetext` со склонением («минус 2,5 диоптрии»).
- Подпись состояния с кроссфейдом 200 мс: 0 — «Стопроцентное зрение»,
  −0,5…−3,0 — «Слабая», −3,5…−6,0 — «Средняя близорукость».
- Толчок-приглашение: через 1,2 с после появления секции значение само
  проходит 0 → −1,0 → 0 (ступенями, spring), один раз; любое
  взаимодействие отменяет. Подсказка «Потяните ползунок» исчезает
  навсегда после первого касания.
- Дисклеймер под подписью состояния со ссылкой на `#cta`.
- Reduced-motion: толчка нет, транзишены мгновенные (JS-ветки + глобальный
  CSS-килсвитч в globals.css), слайдер и клавиатура полностью работают.

### Блок 2 — интеграция и чистка
- `src/components/sections/vision.tsx`: `Compare` → `MyopiaSim`.
- `src/content/home.ts`: новый объект `vision` (heading/body/hint/states/
  disclaimer/disclaimerCta), `labelBefore`/`labelAfter` удалены.
- `src/components/ui/compare.tsx` удалён; импортов и упоминаний не
  осталось (проверено поиском по `src`).
- `CLAUDE.md`: описание подписного эффекта заменено на симулятор;
  добавлена строка про авторский прелоадер «глаза со зрачками»
  (промптам не менять).

### Блок 3 — аудит
Выводы команд — раздел 1. Дополнительно секция прогнана в headless-браузере
(Edge, dev-сервер) на 1440 и 390 со скриншотами и реальными кликами/драгом/
клавиатурой — итоги в чеклисте.

---

## 3. Чеклист (3.2)

- [x] 13 ступеней, снап к 0,5; старт с 0; transition 180 мс — клик в
      середину трека даёт ровно −3,0;
- [x] калибровка: blur совпадает с формулой пиксель в пиксель
      (1184px → 15,39px на −6; 342px → 4,45px на −6); при −1 сцена
      «ещё неплохо», при −6 нечитаема;
- [x] маска глубины: computed-style на оверлее, на скриншотах низ кадра
      (дорожная разметка) заметно резче верха;
- [x] при D=0 оверлей `opacity: 0` — база пиксельно резкая;
- [x] капсула «−2,5 дптр» едет с бегунком, формат с запятой; у краёв
      трека прижимается, чтобы не выходить за контейнер;
- [x] подпись состояния меняется ровно на границе −3,0/−3,5 (проверено
      именно граничным шагом);
- [x] дисклеймер: ссылка ведёт на `#cta`, секция с таким id существует;
- [x] толчок: серия значений 0 → −0,5 → −1 → −0,5 → 0, один раз;
      отмена — очистка таймеров при любом взаимодействии (код);
- [x] подсказка исчезает после первого касания (opacity 0 после клика);
- [x] клавиатура: ←/→, Home/End, `aria-valuetext` («минус 3,5 диоптрии»);
- [x] тач: тап по треку на 390 работает; `touch-action: none` только на
      треке — страница скроллится вне его;
- [x] reduced-motion: толчка нет (D стабильно 0), End → −6 работает,
      blur применяется мгновенно;
- [~] слабый CPU (4x slowdown): не профилировал инструментально, но
      пофреймового blur нет ПО ПОСТРОЕНИЮ — filter пересчитывается только
      на ступенях (максимум 13 раз за весь драг), между ступенями работает
      только spring бегунка (transform). Прошу глянуть на реальном
      телефоне.

Ошибок в консоли нет (desktop / mobile / reduced-motion — кроме п. 4.5
ниже, не относящегося к этому проходу).

---

## 4. Отклонения от промпта

1. **`ink-soft` не существует в палитре проекта** — для цифр шкалы и
   дисклеймера использован `graphite` (#6F6B64, штатный цвет вторичного
   текста — тот же смысл).
2. **Ссылка в дисклеймере** — ссылкой сделана вся фраза «Точные цифры —
   на проверке зрения за 20 минут», а не только слова «проверке зрения»:
   контент лежит в `home.ts` одной строкой, и дробить её на три части
   ради подссылки хрупко (сломается при первой же правке текста
   владельцем). Заодно крупнее цель нажатия. Если нужна ссылка строго
   внутри фразы — сделаю после согласования финального текста.
3. **Клавиатура: добавлены ↑/↓** как дубль ←/→ — ARIA-паттерн слайдера
   требует вертикальные стрелки, скринридеры на Windows ими пользуются.
   Стоило две строки.
4. **`scale-105` на внутреннем слое оверлея** — приём из старой шторки:
   прячет прозрачную кайму, которую blur даёт по краям кадра. В промпте
   не упомянут, без него края «светятся».
5. **Капсула у краёв клампится** (±44px от краёв трека), бегунок при этом
   доезжает до конца — иначе на 390 капсула вылезала бы за контейнер.
6. Скиллы `frontend`/`ux-ui`/`taste`, требуемые CLAUDE.md, в окружении
   не установлены — следовал дизайн-словарю самого CLAUDE.md.

Попутное наблюдение (не чинил, вне объёма): в dev при reduced-motion
React пишет hydration-warning из `hero.tsx:60` (motion-элемент с
`y: 12` рендерится сервером, а клиент при reduce отдаёт обычный тег).
На прод-сборку не влияет, но стоит поправить отдельным проходом.

---

## 5. Вопросы владельцу

1. **Тексты TODO** — согласовать: heading, body, hint, названия
   состояний, дисклеймер и фразу-мост к записи.
2. **Коэффициент 0.013** — моё впечатление: на десктопе попадание
   точное (−1 «ещё неплохо», −3 уже неуютно, −6 пятна света). На 390
   контейнер всего ~342px, и −6 даёт 4,5px — мир размыт, но менее
   драматично, чем на десктопе. Это честная относительная формула, но
   если на телефоне захочется сильнее — можно поднять коэффициент только
   для малых ширин или ввести нижний пол (например, не меньше 7px на −6).
   Решение за вами после живого теста на телефоне.
3. **Старт с 0** — сейчас фото на старте идеально резкое, «портит» мир
   пользователь; приглашает к этому толчок 0 → −1 → 0. Если «сначала
   идеально» покажется тихим — могу вернуть старт с −2,5 (мир сразу
   сломан, тянуть к нулю = «прозреть»). Мне нравится текущий вариант:
   он точнее по смыслу («так видит близорукость»), но решение за вами.

**Напоминание:** посмотрите секцию на 1440 и 390; git — руками
(коммит этого прохода).

---

## 6. Итерация по фидбэку владельца (12.06.2026)

Фидбэк: «Бегунок внизу неплох, но шторка выглядела вау, давай также
градации но со шторкой, увеличь подписи с диоптриями».

### Сделано
Симулятор переработан в гибрид «шторка + градации» (тот же файл
`myopia-sim.tsx`, имя компонента не менялось):
- Вернулась шторка: вертикальная линия с кружком делит кадр, слева —
  мир «без коррекции», справа — резкий; drag/клик в любом месте кадра,
  линия скользит плавно (clip-path, как в старом compare).
- Градации остались: позиция линии задаёт силу близорукости ступенями
  0,5 (0 у левого края … −6 у правого). Blur по-прежнему пересчитывается
  ТОЛЬКО на границах ступеней (transition 180 мс) по той же калибровке
  `0.013 × ширина` с маской глубины и saturate — пофреймового blur нет.
- Капсула с диоптриями увеличена: 10px → 14px (моб.) / 16px (десктоп),
  `bg-ink/85`, едет с линией, у краёв кадра прижимается внутрь, чтобы
  её не срезал overflow.
- Вернулись угловые подписи «Без коррекции» / «В очках «Оптимист»»
  (`labelBefore`/`labelAfter` снова в `home.ts`, TODO: согласовать).
- Старт — середина кадра = −3,0 («сначала сломано» — обе половины мира
  видны сразу, вопрос №3 выше закрыт фидбэком).
- Толчок теперь по шторке: через 1,2 с линия проходит одну ступень
  (−3,0 → −3,5 — подпись мигает «Средняя близорукость») и возвращается.
- Шкала-трек под фото удалена; клавиатура переехала на линию
  (←/→ — ступень 0,5, Home/End — 0/−6, `aria-valuetext` сохранён),
  подпись состояния и дисклеймер остались под фото.
- `home.ts`: body/hint теперь про линию; `vision.tsx` передаёт угловые
  подписи; CLAUDE.md — описание подписного эффекта обновлено.

### Проверки
- `tsc` / `lint` / `build` — зелёные. First Load JS **165 кБ** (+0,1 кБ
  к route-чанку, граница округления против 164 кБ).
- Headless-прогон (Edge, 1440/390/reduced-motion): 31/32 — старт −3,0,
  толчок −3 → −3,5 → −3, клик в 1/6 кадра даёт −1, драг идёт ступенями
  (−6 → −5,5 → … → −3), Home убирает шторку полностью (clip 100%,
  «Стопроцентное зрение»), blur совпадает с формулой пиксель в пиксель
  на обеих ширинах, капсула 16px, подсказка гаснет, reduced-motion без
  толчка и с мгновенными шагами. Единственный фейл — тот же
  pre-existing hydration-warning из `hero.tsx` при reduced-motion
  (см. раздел 4), к симулятору отношения не имеет.

**Напоминание:** гляньте шторку живьём на 1440 и 390; git — руками.
