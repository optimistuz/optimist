# REPORT-DAY — «Парящие оправы» (блоки 1–5)

Сессия продолжена после обрыва: блоки 1–2 и черновик блоков 3–4 были
готовы, эта сессия закрыла проверки блока 3, блок 5 целиком и вылечила
два найденных аудитом дефекта. Откатов не было (ни один блок не дошёл
до трёх неудач).

---

## Сделанное

### Блок 1 — фотослужба (прошлая сессия, здесь — итоги)
| Слот | Вердикт | Нормализация |
|---|---|---|
| `hero-float` | Принят: чёрный фронт + белые дужки, 3/4, жёсткая живая тень (Daniel Balarezo, Pexels) | кроп 6%, gain R/G/B = 1.0699 / 1.0566 / 1.0356 |
| `deco-1` | **БРАК всех кандидатов** — ни один не прошёл нормализацию без выжигания тени (gain > 1.085). Слот пуст: «пустой слот лучше заметного шва» | — |
| `deco-2` | Принят: пара восьмиугольных оправ, чёрный ацетат + золото (Volker Meyer, Pexels) | gain R/G/B = 1.0759 / 1.0759 / 1.0479 |

Чекпойнт владельца (1.5) пройден в прошлой сессии; подтверждение в
этом чате: «Мы выбрали фото». `_cand/` удалена, авторство и gain'ы —
в `src/content/photos.ts`.

### Блок 2 — FloatFrame
`src/components/ui/float-frame.tsx`: изолированная коробка с фоном
секции ВНУТРИ транформ-слоя; вся фокус-грамматика (entrance / focal /
exitDefocus / contrast) — одним `useMotionTemplate` на `<img>`; живая
тень-эллипс (×0.92, «приподнялся» при наклоне); reduced-motion-ветка.
CLAUDE.md, раздел «Парящие оправы» — записан и дополнен (см. ниже).

### Блок 3 — hero
Рамка 4:3, bokeh-двойник и FocalPlane демонтированы (слот `hero` в
карте сохранён — запас). `FloatFrame slot="hero-float"`: desktop
52vw/48vw, rotate −8°, верх под стеклянную шапку, правый край за
вьюпорт; cursorTilt + rotateDrift ВКЛ; entrance "focus" после
прелоадера (один каскад с заголовком); exitDefocus 4px + exitScale
0.97 — рэк-фокус на выходе; тень natural (в кадре своя). Мобайл: 85vw
над заголовком в потоке, без фильтров, параллакс мягче. Прощальная
хореография заголовка (строки на разных скоростях) — в hero.tsx.

### Блок 4 — деко
Точка Б: `deco-2` в «Экспертизе», слева у водяного знака Сивцева,
15vw, rotate −8°, выпуск за левый край, focal="scroll", entrance,
hidden на мобиле, z под контентом.
Точка А (стык манифеста и «Зрения») **не построена**: слот deco-1
пуст после брака кандидатов — «эффекты, требующие фотографий, не
строятся до появления фотографий». На сайте 2 оправы из лимита ≤3.

### Блок 5 — аудит: найдено и вылечено два дефекта

**Дефект 1 — оптимизатор Next ломал рубеж 3.** Первый пиксельный
прогон дал систематический «фон − 1» внутри кадров (245,243,238 против
246,244,239) во всех состояниях. Изоляция (стенды
cdp-filter-experiment / cdp-live-isolate, удалены за ненадобностью):
фильтры, will-change, transform, multiply — невиновны; виновник —
`/_next/image`: WebP-перекодировка (YUV) сдвигает белую точку
255 → 254 при любом q (проверено канвас-ридбеком: оригиналы — честные
255 во всех точках, ВСЕ варианты оптимизатора — 254). Лечение:
`unoptimized` на blend-картинках FloatFrame (проп `quality` снят как
бессмысленный). Цена: hero-float 169 кБ JPEG вместо ~60 кБ WebP; LCP
не деградировал (2332 мс против 2344 мс — доминирует прелоадер).

**Дефект 2 — reduced-motion: оправа была невидима (opacity 0).**
До маунта рендерится анимационная ветка (opacity 0), после маунта
still-ветка подставляла `style={undefined}` — motion НЕ стирает уже
выставленные inline-стили, opacity 0 замерзала навсегда. Лечение:
явные статичные стили (`{ rotate }` на слое, `{ opacity: 1,
filter: "none" }` на img). Заодно закрыта смежная дыра: при смене
desktop→mobile инлайновый фильтр тоже бы замерзал — явный
`filter: "none"`.

Оба факта внесены в CLAUDE.md («четыре рубежа» вместо трёх, правило
явных статичных стилей).

---

## Рубеж 3 — пиксельная приёмка (1440×900, после лечения)

Методика: headless Chrome + CDP (`scripts/cdp-audit.mjs`, без новых
зависимостей — WebSocket-клиент и PNG-декодер на встроенных модулях
Node). Координаты пар вычисляются от живого quad'а оправы
(DOM.getContentQuads), каждая точка проверяется на локальную
однородность (±3px), чтобы не сесть на тень/линию/глиф. Зерно
body::after на время строгого замера отключалось (равномерно поверх
всего вьюпорта, шва создать не может); контрольный прогон С зерном —
допуск ±1 — пройден, т.е. иных источников расхождения нет.

Все пары во всех состояниях: **RGB идентичны канал в канал**
(везде 246,244,239 = #F6F4EF). Выборка замеров (in ↔ out, экранные px):

| Состояние | Пары (выборка из N) |
|---|---|
| hero, покой | (884,110)↔(802,121) · (911,316)↔(829,327) · (1304,446)↔(1329,526) — из 9 пар |
| hero, параллакс + рэк-фокус blur 2.89px | (1097,138)↔(1107,219) · (1266,119)↔(1275,199) · (1401,103)↔(1430,182) — из 6 пар |
| deco-2, центр вьюпорта | (31,351)↔(27,322) · (95,342)↔(91,313) · (148,453)↔(191,447) — из 6 пар |
| deco-2, сдвиг + focal blur 0.78px | (31,607)↔(27,579) · (95,598)↔(91,570) · (148,710)↔(191,704) — из 6 пар |
| hero, середина entrance (opacity 0.18) | 9 пар — идентичны |
| hero, reduced-motion (статика) | 9 пар — идентичны |

Третья оправа отсутствует по делу (deco-1 — брак, слот пуст), так что
«у всех трёх» фактически = у обеих существующих.

---

## Реальные выводы инструментов (5.1)

- `tsc --noEmit` — чисто; `next lint` — без предупреждений.
- `next build` — главная **171 кБ First Load JS**; против базовых
  169 кБ дельта **+2 кБ** (бюджет ≤ +3 кБ — ОК).
- Lighthouse недоступен без новой зависимости (интернет — только
  Pexels) → **замер ручной** через PerformanceObserver по CDP:
  LCP = 2332 мс на локальном прогоне, LCP-элемент — сама hero-оправа
  (`/photos/hero-float.jpg`, priority). Большая часть — прелоадер
  (≤1.8 с по дизайну).

## Чеклист (5.3)

- [x] Нормализация: gain каждого канала ≤ 1.085 (макс. 1.0759), рамка
  ≥ 254, тени живы (отсмотрено глазами в прошлой сессии).
- [x] Рубеж 3: пары идентичны у обеих оправ, в покое и в движении
  (координаты выше; полный JSON воспроизводим скриптом).
- [x] Ни одного белого прямоугольника ни в одной анимации (замеры в
  середине entrance, при активном рэк-фокусе и focal blur).
- [x] Фокус-грамматика: entrance после прелоадера, один каскад с
  заголовком; рэк-фокус на выходе hero (blur 2.89px на скролле 320,
  прощальный scale 0.981 подтверждён computed-матрицей); деко ловит
  резкость у центра (0.14px) и отпускает к кромкам (0.78px на сдвиге);
  все фильтры — один useMotionTemplate, обёрток-фильтров нет.
- [x] Живая тень: на сайте не используется (у hero тень natural из
  кадра) — drop-shadow на blend-картинках отсутствует.
- [x] Дужка под стеклянной шапкой (верхняя кромка quad'а уходит до
  y = −19 над вьюпортом); горизонтального скролла нет на
  360/390/1024/1280/1440/1920; деко и hero-оправа не пересекают текст
  ни на одной из 4 контрольных ширин (SAT-тест quad×rect, 0 пересечений
  даже на 1024, где заход под текст был бы допустим).
- [x] Мобайл: деко скрыты (display:none), у hero-оправы фильтров нет
  (computed filter: none).
- [x] Производительность: анимируемый blur — на 2 img (≤3), капы
  4px/2px соблюдены. Скролл на 4×-замедлении — ручная проверка
  (headless честного замера не даёт).
- [x] Reduced-motion: оправа статична и резка (opacity 1, filter none,
  transform — чистый rotate(−8°)) — после лечения дефекта 2.
- [x] Прелоадер не тронут; вечная анимация одна (подсказка скролла);
  LCP не деградировал.

## Пометки

- Скиллы `frontend` / `ux/ui` / `taste`, обязательные по CLAUDE.md,
  в этой среде не установлены (ни в проекте, ни глобально) — работал
  по дизайн-правилам CLAUDE.md и PROJECT.md, которые их конспектируют.
- Инструменты приёмки оставлены в `scripts/`: `cdp-audit.mjs` (рубеж 4
  и весь прогон), `cdp-whitepoint.mjs` (белая точка отдаваемых
  битмапов — обязателен при добавлении любого нового кадра техники).

## Вопросы владельцу

1. Масштаб и наклон hero-оправы (сейчас 52vw/48vw, −8°) — так?
2. Рэк-фокус на выходе hero: сила 4px норм или мягче?
3. cursorTilt + rotateDrift у героя — живость или суета? (выключаются
   пропсами без правки логики)
4. Третья деко-точка у подвала: искать кадр или достаточно двух?
   (точка А у «Зрения» тоже ждёт кадра — deco-1 забракован целиком)
5. Судьба снятого hero-фото (`/photos/hero.jpg`, клабмастер): слот
   сохранён как запас — оставить или убрать из карты?

**Напоминание:** посмотреть глазами на 1440 и 390; git — руками.
